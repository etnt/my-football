# Secrets — never commit the API key
API-KEY.txt
*.env
.env

# Android signing — never commit keystores or key configs
*.jks
*.keystore
key.properties

# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
# Note: pubspec.lock is intentionally committed for apps (reproducible builds).

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
